services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: kinto-nextjs-template
    container_name: kinto-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
